

what is next js

	-next js is react framework use to build a fast and scalable application
	-React apps run mainly in the browser, while Next.js can run both on the server and in the browser.


for create next js

	
		npx create-next-app@latest myapp
----------------------------------------------------------

server component
app-->page.js -------> this a server component for next js 

	
	-server component only run on a server never on a client(browser)
for example
	-in server component if u use a "console.log " u cant a see in that in a browser instead of u can see in a terminal

---

for create another page in next
	
  -next had a file based routing system
  -u dont need to import the file any ware
-just create a page.js(server component ) in side the folder it automatically execute
  -if hit a url with a folder name u can see the page
-just create a folder and create a server file (page.js)

	-that all u right default react code it will execute




	

navigation
	
	-using link component from next we can routing

	import Link from "next/link";

	using
	
		<Link href="about">about</Link>


----------------------------------------------------------
layout component

	-A layout component wraps pages and provides shared elements like header, footer, and navigation.
	-if we right a footer in layout component
-it will give thos for all page.js inside the folders
----------------------------------------------------------

server components and client components 

next js components are two types client components and server componets 

server componets 
	-they using for data fetching and backend and backend logic for better performence 
	-sice they not using in javascripts browser 
	
client componets 
	-this run on the browser 
	-used to intraction and handle the states and lifecyle and hooks 

"""by default all componensts are server in next js if we want we should make a client components in next js """

----------------------------------------------------------
hooks

usePathname ---------->it give current path for a application  
for example
---------URL: http://localhost:3000/blog/123
it give like this 
---------blog/123

useFormStatus
	useFormStatus is a React hook from react-dom, but it is commonly used in Next.js with server actions to track form submission state like loading or pending.

-------------------
useFormState()   it depricate now they use useActionState ()

	is used to manage form submission state and handle server-side validation results cleanly.
	

----------------------------------------------------------
special convetion files ?

loading.js -------------->next deducted automatically for every witing time it show the loading... page  
page.js ----------------->reprecent a routing and help to create a page
layout.js---------------->it is a shared ui it will share the common componets for every component like header and footer 
error.js----------------->Catches runtime errors in that route
----------------------------------------------------------
suspense componet 

	-suspanse component also work for loading when the data waiting period  
	-different between the loading file and suspense componnent 

loading 
	its a autometically loading every file when a data waiting peirod 

suspese 
	-we need to do mannually and for spesific componet
	-if want only a spesific component need to load for a waiting period we can use suspese 

syntex
	<suspense fallback={<p >....loading</p>}>
		<Main />
	<suspense />
----------------------------------------------------------
how to connect database in nextjs 

install the mogoDB
	npm install mongodb

using "MongoClient Connect "
	MongoClient Connect('conect URL')
























